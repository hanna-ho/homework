# Add the target host(s) below, or pass your own inventory with:
#   ansible-playbook -i your_inventory.ini site.yml
#
# Example for running on the local machine:
#   localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3
#
# Example for running against a remote lab host:
#   servera.lab.example.com

[webservers]
servera.lab.example.com