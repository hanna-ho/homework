---
- name: Deploy static web pages
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "{{ site_html_dir }}/{{ item }}"
    owner: "{{ web_user }}"
    group: "{{ web_user_primary_group }}"
    mode: '0644'
    setype: "{{ selinux_html_type }}"
  loop: "{{ web_pages }}"
